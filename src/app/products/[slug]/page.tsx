import {notFound} from "next/navigation";import {products} from "@/data/products";import {Container} from "@/components/ui/Container";import {Price} from "@/components/ui/Price";import {AddToCartButton} from "@/components/commerce/AddToCartButton";export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <Container className="py-10 md:py-16"><div className="grid gap-10 md:grid-cols-2"><div className="bg-[var(--surface)]"><img src={p.image} alt={p.name} className="aspect-square w-full object-cover"/></div><div className="flex flex-col justify-center"><p className="eyebrow">{p.category} / {p.collection}</p><h1 className="display mt-5 text-6xl md:text-8xl">{p.name}</h1><div className="mt-7"><Price value={p.price} compareAt={p.compareAt}/></div><p className="mt-7 max-w-lg text-sm leading-7 text-[var(--muted)]">{p.description}</p><div className="mt-9"><p className="eyebrow mb-3">Select size</p><div className="grid max-w-sm grid-cols-3 gap-2">{p.sizes.map(s=><AddToCartButton key={s} product={p} size={s}/>)}</div></div></div></div></Container>}