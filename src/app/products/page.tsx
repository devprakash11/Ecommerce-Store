import {Container} from "@/components/ui/Container";import {ProductGrid} from "@/components/commerce/ProductGrid";import {products} from "@/data/products";export default async function Products({searchParams}:{searchParams:Promise<{q?:string;category?:string}>}){const sp=await searchParams;const q=(sp.q||"").toLowerCase();const list=products.filter(p=>(!q||p.name.toLowerCase().includes(q)||p.category.toLowerCase().includes(q))&&(!sp.category||p.category.toLowerCase()===sp.category.toLowerCase()));return <Container className="py-12 md:py-20"><p className="eyebrow">Shop</p><h1 className="display mt-5 text-7xl md:text-[9rem]">ALL<br/>MOVEMENT.</h1><div className="mt-8 flex flex-wrap gap-2">{["All","Running","Trail","Court","Lifestyle","Outdoor"].map(c=><a key={c} href={c==="All"?"/products":`/products?category=${c}`} className="border border-[var(--line)] px-4 py-2 text-[10px] font-bold uppercase tracking-[.14em]">{c}</a>)}</div><div className="mt-10"><ProductGrid products={list}/>{!list.length&&<p className="py-20 text-center text-[var(--muted)]">No products found.</p>}</div></Container>}