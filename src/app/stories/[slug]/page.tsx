import {notFound} from "next/navigation";import {Container} from "@/components/ui/Container";const stories:Record<string,[string,string[]]>={rotation:["How to build a rotation",["A useful rotation starts with one dependable daily pair.","Add a technical runner for high-mileage days, then a trail or court silhouette when the surface changes."]],"low-profile":["Why low-profile sneakers are back",["Low-profile shapes bring the visual weight closer to the ground.","The new generation mixes archival proportions with modern cushioning."]],"movement-edit":["The daily movement edit",["The best everyday sneaker is the one you stop thinking about.","Comfort, traction and a flexible upper make a difference across long days."]]};export default async function Story({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=stories[slug];if(!s)notFound();return <Container className="py-12 md:py-20"><p className="eyebrow">Journal</p><h1 className="display mt-5 max-w-6xl text-7xl md:text-[9rem]">{s[0].toUpperCase()}</h1><div className="mx-auto mt-14 max-w-2xl space-y-6 text-lg leading-9">{s[1].map(p=><p key={p}>{p}</p>)}</div></Container>}