import Link from "next/link";import {Container} from "@/components/ui/Container";const stories=[["How to build a rotation","Guide","rotation"],["Why low-profile sneakers are back","Culture","low-profile"],["The daily movement edit","Journal","movement-edit"]];export default function Stories(){return <Container className="py-12 md:py-20"><p className="eyebrow">Stories</p><h1 className="display mt-5 text-7xl md:text-[9rem]">JOURNAL.</h1><div className="mt-12 grid gap-5 md:grid-cols-3">{stories.map(([title,tag,slug])=><Link key={slug} href={`/stories/${slug}`} className="border-t border-[var(--line)] pt-5"><p className="eyebrow text-[var(--muted)]">{tag}</p><h2 className="mt-4 text-3xl font-bold">{title}</h2><p className="mt-8 text-sm text-[var(--muted)]">Read the story →</p></Link>)}</div></Container>}