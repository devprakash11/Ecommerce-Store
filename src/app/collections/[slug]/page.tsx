import {notFound} from "next/navigation";import {products} from "@/data/products";import {Container} from "@/components/ui/Container";import {ProductGrid} from "@/components/commerce/ProductGrid";const names:Record<string,string>={performance:"Performance",outdoor:"Outdoor",everyday:"Everyday",court:"Court"};export default async function Collection({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const name=names[slug];if(!name)notFound();const list=products.filter(p=>p.collection.toLowerCase()===slug||p.category.toLowerCase()===slug);return <Container className="py-12 md:py-20"><p className="eyebrow">Collection</p><h1 className="display mt-5 text-7xl md:text-[9rem]">{name.toUpperCase()}.</h1><p className="mt-6 max-w-xl text-sm leading-7 text-[var(--muted)]">A focused edit of silhouettes built around {name.toLowerCase()} movement.</p><div className="mt-12"><ProductGrid products={list}/></div></Container>}