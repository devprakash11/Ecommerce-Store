import Link from "next/link";import {Container} from "@/components/ui/Container";export default function Account(){return <Container className="py-20"><p className="eyebrow">Account</p><h1 className="display mt-5 text-7xl">YOUR<br/>SPACE.</h1><div className="mt-10 grid gap-3 md:grid-cols-2">{["orders","profile","addresses","wishlist"].map(x=><Link key={x} href={`/account/${x}`} className="border border-[var(--line)] p-6 font-bold uppercase tracking-[.1em]">{x}</Link>)}</div></Container>}