"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Search(){const[q,setQ]=useState("");const router=useRouter();return <div className="container-site py-24 md:py-40"><p className="eyebrow">Search</p><h1 className="display mt-5 text-7xl md:text-[10rem]">FIND IT.</h1><form onSubmit={e=>{e.preventDefault();router.push(`/products?q=${encodeURIComponent(q)}`)}} className="mt-12 flex border-b-2 border-black"><input aria-label="Search products" autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="SEARCH PRODUCTS" className="min-w-0 flex-1 bg-transparent py-5 text-xl font-bold uppercase outline-none"/><button className="px-3 text-xs font-bold uppercase tracking-[.15em]">Search</button></form></div>}