import Link from "next/link";import type {Product} from "@/types/product";import {Price} from "@/components/ui/Price";export function ProductCard({product}:{product:Product}){return <article className="group"><Link href={`/products/${product.slug}`}><div className="relative aspect-square overflow-hidden bg-[var(--surface)]"><img src={product.image} alt={product.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.035]"/>{product.compareAt&&<span className="eyebrow absolute left-3 top-3 bg-[var(--lime)] px-2 py-1">New</span>}</div><div className="flex items-start justify-between gap-4 py-4"><div><h3 className="font-bold">{product.name}</h3><p className="mt-1 text-xs uppercase tracking-[.12em] text-[var(--muted)]">{product.category}</p></div><Price value={product.price} compareAt={product.compareAt}/></div></Link></article>}