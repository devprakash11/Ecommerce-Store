"use client";import {useState} from "react";import type {Product} from "@/types/product";import {useCartStore} from "@/features/cart/store/cart-store";export function AddToCartButton({product,size}:{product:Product;size:number}){const add=useCartStore(s=>s.add);const[done,setDone]=useState(false);return <button className="h-12 w-full bg-black text-xs font-bold uppercase tracking-[.15em] text-white" onClick={()=>{add(product,size);setDone(true);setTimeout(()=>setDone(false),1000)}}>{done?"Added to bag":"Add to bag"}</button>}