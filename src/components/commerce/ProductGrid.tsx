import type {Product} from "@/types/product";import {ProductCard} from "./ProductCard";export function ProductGrid({products}:{products:Product[]}){return <div className="grid grid-cols-2 gap-x-3 gap-y-8 md:grid-cols-3 lg:gap-x-5">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>}